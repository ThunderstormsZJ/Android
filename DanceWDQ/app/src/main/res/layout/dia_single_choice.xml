<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="225dp"
    android:background="@android:color/white"
    android:orientation="vertical"
    android:paddingBottom="5dp">

    <cn.spinsoft.wdq.widget.wheel.RecycleWheelView
        android:id="@+id/single_choice_wheel"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_alignParentBottom="true"
        app:recycleWheelLineColor="@color/divider_gray"
        app:recycleWheelLineThickness="1dp" />

    <TextView
        android:id="@+id/single_choice_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:padding="25dp"
        android:text="取消"
        android:textColor="@color/colorTextGray"
        android:textSize="@dimen/text_size_28" />

    <TextView
        android:id="@+id/single_choice_confirm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:padding="25dp"
        android:text="确定"
        android:textColor="@color/colorTextOrange"
        android:textSize="@dimen/text_size_28" />

</RelativeLayout>